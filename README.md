# SkillBot — Версия 2.2

SkillBot — это мощный автоматизированный помощник для игр, который предоставляет два основных функционала:
- **Skillbot** — автоматическое использование навыков по очереди с управлением кулдаунами
- **Autopot** — автоматическое восстановление здоровья при снижении ХП

## 📁 Структура репозитория
```
.
├── __version__.py               # Номер версии
├── main.py                      # Точка входа
├── README.md                    # Этот файл
├── requirements.txt             # Зависимости
├── CODE_INDEX.md                # Индекс кода
├── icon.png                     # Иконка приложения
│
├── assets/                      # Ресурсы
│   ├── __init__.py
│   └── help.html                # HTML справка
│
├── config/                      # Конфигурация
│   ├── __init__.py
│   └── settings.json            # Параметры приложения
│
├── core/                        # Логика бота и автопота
│   ├── __init__.py
│   ├── bot.py                   # SkillBot класс (основной цикл)
│   ├── autopot.py               # AutopotThread класс (мониторинг пикселя)
│   └── keyboard_handler.py      # Управление хуками клавиатуры
│
├── models/                      # Модели данных
│   ├── __init__.py
│   ├── profile.py               # Профиль персонажа
│   └── skill.py                 # Модель скилла
│
├── profiles/                    # Профили пользователей (JSON)
│   ├── default.json
│   └── example_profile_full.json
│
├── scripts/                     # Служебные скрипты
│   ├── __init__.py
│   ├── build_index.py           # Скрипт построения индекса
│   ├── import_check.py          # Скрипт проверки импортов
│   └── prune_root.py            # Скрипт очистки корня (архивирование)
│
├── ui/                          # Пользовательский интерфейс
│   ├── __init__.py
│   ├── main_window.py           # Главное окно приложения
│   ├── styles.py                # Современные стили CSS
│   ├── style_modules/           # Модульные стили
│   │   ├── __init__.py
│   │   ├── base.py              # Базовые стили
│   │   ├── colors.py            # Цвета
│   │   ├── components.py        # Стили компонентов
│   │   ├── responsive.py        # Адаптивные стили
│   │   └── window.py            # Стили окна
│   ├── tabs/                    # Вкладки интерфейса
│   │   ├── __init__.py
│   │   ├── autopot_tab.py       # Вкладка автопота
│   │   ├── help_tab.py          # Справка
│   │   ├── main_tab.py          # Главная вкладка
│   │   ├── overlay_tab.py       # Оверлей
│   │   └── settings_tab.py      # Параметры
│   └── widgets/                 # Пользовательские виджеты
│       ├── __init__.py
│       ├── click_overlay.py     # Оверлей клика
│       ├── key_capture_button.py # Кнопка захвата клавиш
│       ├── overlay_window.py    # Окно оверлея
│       └── select_all_lineedit.py # Поле ввода с выделением
│
└── utils/                       # Утилиты и вспомогательные функции
    ├── __init__.py
    ├── json_helper.py           # Работа с JSON
    ├── logger.py                # Логирование
    └── profile_manager.py       # Менеджер профилей
```

## 📦 Установка (Windows PowerShell)

### 1. Проверка Python
```powershell
python --version
```
Требуется Python 3.9+. Рекомендуемая версия: `C:/Users/Admin/AppData/Local/Programs/Python/Python39/python.exe`

### 2. Установка зависимостей
```powershell
python -m pip install -r requirements.txt
```

### 3. Запуск приложения
```powershell
python main.py
```

## 🚀 Быстрый старт

### Первый запуск
1. Откройте вкладку **Settings** и введите название окна вашей игры (например, "Lost Ark")
2. На вкладке **Autopot** нажмите "Выбрать точку" и кликните на индикатор здоровья при полном ХП
3. На вкладке **Skillbot** загрузите или создайте профиль с очередью навыков
4. Используйте горячие клавиши **F8** (Skillbot) и **F9** (Autopot) для управления

### Проверка целостности (import-check)
```powershell
python -c "from ui.main_window import MainWindow; print('✓ IMPORTS_OK')"
```

## ⚙️ Основные компоненты

### core/bot.py - SkillBot
- **Назначение:** Автоматическое использование навыков по очереди с управлением кулдаунами
- **Горячая клавиша:** F8 (по умолчанию)
- **Сигналы:** `update_signal`, `log_signal`
- **Методы:** `set_skills()`, `start()`, `stop()`, `toggle()`

### core/autopot.py - AutopotThread
- **Назначение:** Мониторинг цвета пикселя и автоматическое восстановление ХП
- **Горячая клавиша:** F9 (по умолчанию)
- **Параметры:** координаты пикселя, цвет, чувствительность, кулдаун
- **Методы:** `set_params()`, `run()`, `start()`, `stop()`

### ui/main_window.py - MainWindow
- **Назначение:** Главное окно приложения, управление вкладками и логикой
- **Вкладки:** Main, Settings, Autopot, Skillbot, Help, Overlay
- **Методы:** `update_log()`, `update_status()`, `save_profile()`, `load_profile()`

## 📋 Пример профиля (JSON)

Профили сохраняются в `profiles/` в формате JSON:

```json
{
  "name": "Example Profile",
  "skills": [
    {"id": 1, "key": "q", "cooldown": 5000},
    {"id": 2, "key": "w", "cooldown": 8000},
    {"id": 3, "key": "e", "cooldown": 3000}
  ],
  "bot": {
    "enabled": true,
    "hotkey": "f8",
    "pause_on_input": true,
    "target_window": "Lost Ark"
  },
  "autopot": {
    "enabled": true,
    "hotkey": "f9",
    "coords": [960, 540],
    "color": [255, 200, 100],
    "press_key": "f",
    "cd": 0.25,
    "thresh": 15
  }
}
```

Полный пример находится в `profiles/example_profile_full.json`.

## 🔧 Отладка

### GUI не запускается
- Убедитесь, что установлены все зависимости: `pip install -r requirements.txt`
- Проверьте консоль на ошибки импорта
- Если ошибка с `keyboard` или `pynput` — запустите PowerShell от администратора

### Горячие клавиши не работают
- Проверьте, активно ли окно приложения (оно должно быть в фокусе)
- Убедитесь, что целевое окно установлено правильно
- Перезагрузите приложение, если хуки не регистрируются

### Автопот часто ошибается
- Переделайте выбор точки цвета (кликните кнопку ещё раз при полном ХП)
- Попробуйте увеличить чувствительность (значение в поле "Чувствительность")
- Убедитесь, что выбрали точку именно при 100% ХП

### Скилбот не нажимает клавиши
- Проверьте очередь навыков (должны быть клавиши, разделённые точкой с запятой)
- Убедитесь, что интервал между нажатиями достаточен (минимум 100 мс)
- Проверьте, что целевое окно активно

## ⌨️ Горячие клавиши (по умолчанию)

| Клавиша | Функция |
|---------|---------|
| **F8** | Вкл/Выкл Skillbot |
| **F9** | Вкл/Выкл Autopot |
| **F12** | Экстренная остановка (все боты) |

Горячие клавиши можно переконфигурировать в вкладке Settings.

## 📚 Справка в приложении

Полная справка с примерами и FAQ доступна в вкладке **Help**. Там находится:
- Подробные инструкции по использованию каждой вкладки
- Описание всех параметров
- Часто задаваемые вопросы
- Решение типовых проблем

## 🔄 Восстановление старых файлов

Все удалённые топ‑левел файлы сохранены в `archive/root_backup/`. Чтобы откатить:

```powershell
Copy-Item -Path "archive/root_backup/*" -Destination "./" -Recurse
```

## 🛠️ Разработка

### Структура тестирования
```powershell
# Проверка импортов
python -c "from ui.main_window import MainWindow; print('✓ OK')"

# Проверка модулей
python -c "from core.bot import SkillBot; from core.autopot import AutopotThread; print('✓ OK')"
```

### Добавление нового модуля
1. Создайте файл в соответствующей папке (`core/`, `ui/`, `models/`, `utils/`)
2. Добавьте импорт в `__init__.py` пакета (если нужен)
3. Обновите импорты в файлах, использующих новый модуль
4. Проверьте импорты: `python -c "from <package> import <module>; print('✓ OK')"`

## 📖 Дополнительная информация

### Требуемые зависимости (requirements.txt)
```
PyQt6
keyboard==0.13.5
pynput==1.7.6
mss==6.1
numpy==1.21.6
psutil==5.8.0
pywin32==303
```

### Версии Python
- **Минимум:** Python 3.9
- **Рекомендуется:** Python 3.9.x или 3.10.x
- **Windows:** PowerShell 5.1+

## 📝 История изменений

### 2.2 (Текущая версия)
- ✅ Добавлена поддержка русского языка для улучшения поиска и нахождения контента
- ✅ Обновлен поисковый индекс с поддержкой кириллицы
- ✅ Полностью переработанная архитектура с модульной структурой
- ✅ Добавлены новые функции управления ботами
- ✅ Улучшенная система профилей
- ✅ Добавлена возможность оверлея
- ✅ Улучшена система горячих клавиш
- ✅ Добавлена функция паузы при вводе
- ✅ Полностью обновленная документация
- ✅ Современный и адаптивный дизайн интерфейса с использованием CSS-стилей
- ✅ Плавные анимации и переходы между состояниями
- ✅ Поддержка различных размеров экранов и разрешений

- ✅ Установка иконки приложения во всех возможных местах
- ✅ Исправление предупреждений о неизвестных свойствах CSS

### 1.0 beta (Предыдущая версия)
- ✅ Рефакторинг структуры пакетов
- ✅ Очистка топ-левел файлов
- ✅ Создание документации
- ✅ Добавление справки (help.html)
- ✅ Примеры профилей и конфигурации

## 🤝 Поддержка и контакты

Проект — приватный рабочий репозиторий. При возникновении вопросов проверьте:
1. Консоль логов в приложении (вкладка Main)
2. Справку в приложении (вкладка Help)
3. Этот README файл

---
**Последнее обновление:** 29 января 2025 г.
**Версия:** 2.2
**SkillBot** © 2025
**Автор:** FreePing
