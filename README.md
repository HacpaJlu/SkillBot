# SkillBot — Автоматизированный игровой ассистент

**Версия:** 3.0.0  
**Автор:** FreePing  
**Дата релиза:** 29 января 2025

## 📋 Описание проекта

SkillBot — это продвинутый автоматизированный ассистент для игр, разработанный для упрощения игрового процесса за счёт автоматизации рутинных действий. Приложение предоставляет два основных функционала:

- **Skillbot** — автоматическое использование навыков по заданной очереди с учётом кулдаунов
- **Autopot** — автоматическое восстановление здоровья при снижении ХП до заданного уровня

## 🌟 Особенности

- **Модульная архитектура** — чётко разделённые компоненты для лёгкого расширения и сопровождения
- **Гибкие профили** — настройка под различные игры и игровые стили
- **Интуитивный интерфейс** — современный дизайн с поддержкой тем оформления
- **Кастомизация горячих клавиш** — возможность переназначения клавиш под себя
- **Оверлей для точного позиционирования** — визуальная помощь при настройке
- **Поддержка паузы при вводе** — безопасная остановка при переключении между приложениями

## 📁 Структура проекта

```
.
├── __version__.py               # Номер версии и информация о проекте
├── main.py                      # Точка входа в приложение
├── README.md                    # Документация (этот файл)
├── requirements.txt             # Зависимости проекта
├── CODE_INDEX.md                # Индекс кода для поиска
├── icon.png                     # Иконка приложения
│
├── assets/                      # Ресурсы приложения
│   ├── __init__.py
│   └── help.html                # HTML-справка
│
├── config/                      # Конфигурация приложения
│   ├── __init__.py
│   └── settings.json            # Параметры приложения по умолчанию
│
├── core/                        # Ядро приложения
│   ├── __init__.py
│   ├── bot.py                   # SkillBot - основной класс навыкового бота
│   ├── autopot.py               # AutopotThread - поток автопота
│   ├── keyboard_handler.py      # KeyboardHook - обработчик клавиш
│   └── mouse_handler.py         # MouseHandler - обработчик мыши
│
├── models/                      # Модели данных
│   ├── __init__.py
│   ├── profile.py               # Profile - модель профиля пользователя
│   └── skill.py                 # Skill - модель навыка
│
├── profiles/                    # Профили пользователей (JSON)
│   ├── default.json             # Профиль по умолчанию
│   └── example_profile_full.json # Полный пример профиля
│
├── scripts/                     # Служебные скрипты
│   ├── __init__.py
│   ├── build_index.py           # Построение индекса кода
│   ├── import_check.py          # Проверка импортов
│   └── prune_root.py            # Очистка корневой директории
│
├── ui/                          # Пользовательский интерфейс
│   ├── __init__.py
│   ├── main_window.py           # Главное окно приложения
│   ├── styles.py                # CSS-стили интерфейса
│   ├── style_modules/           # Модульные стили
│   │   ├── __init__.py
│   │   ├── base.py              # Базовые стили
│   │   ├── colors.py            # Цветовая палитра
│   │   ├── components.py        # Стили компонентов
│   │   ├── responsive.py        # Адаптивные стили
│   │   └── window.py            # Стили окна
│   ├── tabs/                    # Вкладки интерфейса
│   │   ├── __init__.py
│   │   ├── autopot_tab.py       # Вкладка автопота
│   │   ├── help_tab.py          # Вкладка справки
│   │   ├── main_tab.py          # Главная вкладка
│   │   ├── overlay_tab.py       # Вкладка оверлея
│   │   └── settings_tab.py      # Вкладка настроек
│   └── widgets/                 # Пользовательские виджеты
│       ├── __init__.py
│       ├── click_overlay.py     # Оверлей для кликов
│       ├── key_capture_button.py # Кнопка захвата клавиш
│       ├── overlay_window.py    # Окно оверлея
│       ├── roi_overlay.py       # ROI-оверлей (область интереса)
│       └── select_all_lineedit.py # Поле ввода с выделением
│
└── utils/                       # Вспомогательные утилиты
    ├── __init__.py
    ├── json_helper.py           # Работа с JSON-файлами
    ├── logger.py                # Система логирования
    └── profile_manager.py       # Менеджер профилей
```

## 📦 Установка и запуск

### Требования

- **Python:** 3.9 или выше
- **ОС:** Windows 7/8/10/11 (основная поддержка)
- **Рекомендуемая версия Python:** 3.9.x или 3.10.x

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Запуск приложения

```bash
python main.py
```

## 🚀 Быстрый старт

### 1. Настройка целевого окна

1. Откройте вкладку **Settings**
2. Введите точное название окна вашей игры (например, "Lost Ark")
3. Убедитесь, что приложение может найти целевое окно

### 2. Настройка Autopot

1. Перейдите на вкладку **Autopot**
2. Нажмите кнопку "Выбрать точку" и кликните на индикатор здоровья при полном ХП
3. Настройте чувствительность и кулдаун
4. Укажите клавишу для восстановления здоровья (например, "f")

### 3. Настройка Skillbot

1. Перейдите на вкладку **Skillbot**
2. Загрузите или создайте профиль с очередью навыков
3. Настройте кулдауны для каждого навыка
4. Убедитесь, что навыки разделены точкой с запятой

### 4. Управление

- **F8** — вкл/выкл Skillbot
- **F9** — вкл/выкл Autopot
- **F12** — экстренная остановка всех ботов

## ⚙️ Функциональные возможности

### Skillbot (Навыковый бот)

Skillbot автоматически использует навыки в заданной последовательности с учётом кулдаунов. Основные возможности:

- **Очередь навыков** — навыки используются в заданном порядке
- **Управление кулдаунами** — система отслеживает время восстановления
- **Пауза при вводе** — безопасная остановка при переключении окон
- **Настройка горячих клавиш** — возможность переназначения
- **Тайминги** — точное управление интервалами между навыками

### Autopot (Автоматический пот)

Autopot мониторит цвет пикселя индикатора здоровья и автоматически восстанавливает его при снижении. Основные возможности:

- **Точечный мониторинг** — отслеживание конкретного пикселя
- **Чувствительность** — настройка точности определения цвета
- **Кулдаун** — предотвращение частых нажатий
- **Кастомизация клавиши** — выбор клавиши для восстановления
- **Визуальная настройка** — оверлей для точного выбора точки

### Профили

Система профилей позволяет сохранять и переключаться между различными конфигурациями:

- **JSON-формат** — легко редактируемые файлы
- **Автосохранение** — профили сохраняются автоматически
- **Шаблоны** — готовые примеры для различных ситуаций
- **Импорт/экспорт** — обмен профилями между пользователями

## 📋 Пример профиля

```json
{
  "name": "Example Profile",
  "description": "Профиль для демонстрации возможностей",
  "skills": [
    {
      "id": 1,
      "key": "q",
      "cooldown": 5000,
      "name": "Fireball",
      "description": "Основной навык"
    },
    {
      "id": 2,
      "key": "w",
      "cooldown": 8000,
      "name": "Heal",
      "description": "Навык исцеления"
    },
    {
      "id": 3,
      "key": "e",
      "cooldown": 3000,
      "name": "Shield",
      "description": "Защита"
    }
  ],
 "bot": {
    "enabled": true,
    "hotkey": "f8",
    "pause_on_input": true,
    "target_window": "Lost Ark",
    "skill_delay": 100,
    "loop_delay": 50
  },
  "autopot": {
    "enabled": true,
    "hotkey": "f9",
    "coords": [960, 540],
    "color": [255, 200, 100],
    "press_key": "f",
    "cd": 0.25,
    "thresh": 15,
    "hp_threshold": 70
  }
}
```

## 🔧 Настройки и параметры

### Параметры Skillbot

| Параметр | Описание | Значение по умолчанию |
|----------|----------|----------------------|
| `enabled` | Включен ли бот | `true` |
| `hotkey` | Горячая клавиша | `"f8"` |
| `pause_on_input` | Пауза при вводе | `true` |
| `target_window` | Название целевого окна | `"Lost Ark"` |
| `skill_delay` | Задержка между навыками (мс) | `100` |
| `loop_delay` | Задержка между циклами (мс) | `50` |

### Параметры Autopot

| Параметр | Описание | Значение по умолчанию |
|----------|----------|----------------------|
| `enabled` | Включен ли автопот | `true` |
| `hotkey` | Горячая клавиша | `"f9"` |
| `coords` | Координаты точки мониторинга | `[960, 540]` |
| `color` | Цвет при полном ХП | `[255, 200, 100]` |
| `press_key` | Клавиша для восстановления | `"f"` |
| `cd` | Кулдаун между нажатиями (сек) | `0.25` |
| `thresh` | Чувствительность цвета | `15` |
| `hp_threshold` | Порог ХП для активации (%) | `70` |

## 📚 Документация API

### Core-модули

#### `core/bot.py` - SkillBot

```python
class SkillBot:
    def __init__(self, skills: List[Skill], target_window: str):
        """Инициализация навыкового бота"""
    
    def set_skills(self, skills: List[Skill]):
        """Установка очереди навыков"""
    
    def start(self):
        """Запуск бота"""
    
    def stop(self):
        """Остановка бота"""
    
    def toggle(self):
        """Переключение состояния бота"""
```

#### `core/autopot.py` - AutopotThread

```python
class AutopotThread(QThread):
    def __init__(self, coords: Tuple[int, int], color: Tuple[int, int, int], 
                 press_key: str, cd: float, thresh: int):
        """Инициализация автопота"""
    
    def set_params(self, coords, color, press_key, cd, thresh):
        """Установка параметров"""
    
    def run(self):
        """Основной цикл мониторинга"""
```

## 🔍 Отладка и устранение неполадок

### Частые проблемы и решения

#### GUI не запускается
- Убедитесь, что установлены все зависимости: `pip install -r requirements.txt`
- Проверьте консоль на ошибки импорта
- Если ошибка с `keyboard` или `pynput` — запустите PowerShell от администратора

#### Горячие клавиши не работают
- Проверьте, активно ли окно приложения (оно должно быть в фокусе)
- Убедитесь, что целевое окно установлено правильно
- Перезагрузите приложение, если хуки не регистрируются

#### Автопот часто ошибается
- Переделайте выбор точки цвета (кликните кнопку ещё раз при полном ХП)
- Попробуйте увеличить чувствительность (значение в поле "Чувствительность")
- Убедитесь, что выбрали точку именно при 100% ХП

#### Скилбот не нажимает клавиши
- Проверьте очередь навыков (должны быть клавиши, разделённые точкой с запятой)
- Убедитесь, что интервал между нажатиями достаточен (минимум 100 мс)
- Проверьте, что целевое окно активно

## 🛠️ Разработка

### Тестирование

```bash
# Проверка импортов
python -c "from ui.main_window import MainWindow; print('✓ IMPORTS_OK')"

# Проверка модулей
python -c "from core.bot import SkillBot; from core.autopot import AutopotThread; print('✓ OK')"

# Полная проверка
python -c "
from core.bot import SkillBot
from core.autopot import AutopotThread
from models.skill import Skill
from models.profile import Profile
print('✓ FULL_IMPORTS_OK')
"
```

### Добавление новых функций

1. Создайте файл в соответствующей папке (`core/`, `ui/`, `models/`, `utils/`)
2. Добавьте импорт в `__init__.py` пакета (если нужен)
3. Обновите импорты в файлах, использующих новый модуль
4. Проверьте импорты: `python -c "from <package> import <module>; print('✓ OK')"`

### Структура тестирования

```python
# Пример тестирования SkillBot
from core.bot import SkillBot
from models.skill import Skill

# Создание навыков
skills = [
    Skill(id=1, key='q', cooldown=5000),
    Skill(id=2, key='w', cooldown=8000)
]

# Создание бота
bot = SkillBot(skills=skills, target_window="Test Game")

# Запуск и остановка
bot.start()
bot.stop()
```

## 📦 Зависимости

| Библиотека | Версия | Назначение |
|------------|--------|------------|
| PyQt6 | latest | Графический интерфейс |
| PyQt6-WebEngine | latest | HTML-виджеты |
| keyboard | 0.13.5 | Обработка горячих клавиш |
| pynput | 1.7.6 | Мониторинг ввода |
| mss | 6.1 | Скриншоты экрана |
| numpy | 1.21.6 | Математические операции |
| psutil | 5.8.0 | Системная информация |
| pywin32 | 303 | Windows API |
| opencv-python | 4.8.1.78 | Обработка изображений |
| pyautogui | 0.9.54 | Автоматизация действий |

## 📝 История изменений

### 3.0.0 (Текущая версия)
- ✅ Полностью переписанная архитектура с модульной структурой
- ✅ Добавлена поддержка русского языка для улучшения поиска и нахождения контента
- ✅ Обновлен поисковый индекс с поддержкой кириллицы
- ✅ Добавлены новые функции управления ботами
- ✅ Улучшенная система профилей
- ✅ Добавлена возможность оверлея
- ✅ Улучшена система горячих клавиш
- ✅ Добавлена функция паузы при вводе
- ✅ Полностью обновленная документация
- ✅ Современный и адаптивный дизайн интерфейса с использованием CSS-стилей
- ✅ Плавные анимации и переходы между состояниями
- ✅ Поддержка различных размеров экранов и разрешений
- ✅ Установка иконки приложения во всех возможных местах
- ✅ Исправление предупреждений о неизвестных свойствах CSS
- ✅ Добавлены новые виджеты для точной настройки
- ✅ Улучшена система логирования
- ✅ Добавлена поддержка ROI (области интереса) для автопота

### 2.2
- ✅ Добавлена поддержка русского языка для улучшения поиска и нахождения контента
- ✅ Обновлен поисковый индекс с поддержкой кириллицы
- ✅ Полностью переработанная архитектура с модульной структурой
- ✅ Добавлены новые функции управления ботами
- ✅ Улучшенная система профилей
- ✅ Добавлена возможность оверлея
- ✅ Улучшена система горячих клавиш
- ✅ Добавлена функция паузы при вводе
- ✅ Полностью обновленная документация
- ✅ Современный и адаптивный дизайн интерфейса с использованием CSS-стилей
- ✅ Плавные анимации и переходы между состояниями
- ✅ Поддержка различных размеров экранов и разрешений
- ✅ Установка иконки приложения во всех возможных местах
- ✅ Исправление предупреждений о неизвестных свойствах CSS

### 1.0 beta
- ✅ Рефакторинг структуры пакетов
- ✅ Очистка топ-левел файлов
- ✅ Создание документации
- ✅ Добавление справки (help.html)
- ✅ Примеры профилей и конфигурации

## 🤝 Поддержка и контакты

Проект — приватный рабочий репозиторий. При возникновении вопросов проверьте:

1. Консоль логов в приложении (вкладка Main)
2. Справку в приложении (вкладка Help)
3. Этот README файл

Для дополнительной поддержки обращайтесь к автору проекта.

---

**SkillBot** © 2025  
**Автор:** FreePing  
**Версия:** 3.0.0
